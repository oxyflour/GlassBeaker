{
  "name": "glassbeaker-desktop",
  "version": "0.1.0",
  "author": "GlassBeaker",
  "description": "Minimal Next.js + Electron desktop shell",
  "main": "src/main.cjs",
  "scripts": {
    "dev": "electron .",
    "package:dir": "pnpm --dir ../python build && electron-builder --dir --publish never",
    "dist": "pnpm --dir ../python build && electron-builder --publish never"
  },
  "build": {
    "appId": "com.glassbeaker.app",
    "productName": "GlassBeaker",
    "asar": false,
    "directories": {
      "output": "release"
    },
    "files": [
      "package.json",
      "src/**/*"
    ],
    "extraResources": [
      {
        "from": "../web/.next/standalone",
        "to": "web",
        "filter": [
          "**/*"
        ]
      },
      {
        "from": "../python/dist",
        "to": "python",
        "filter": [
          "**/*"
        ]
      }
    ],
    "win": {
      "signAndEditExecutable": false,
      "target": [
        "nsis"
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  },
  "devDependencies": {
    "electron": "41.0.4",
    "electron-builder": "26.8.1"
  }
}
